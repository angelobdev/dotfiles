#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox

# System
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1	# Polkit
exec-once = $launchers/mako.sh		                                    # Notification Daemon
exec-once = $launchers/waybar.sh	                                    # Status Bar
exec-once = hyprpaper 			                                        # Wallpaper Engine
exec-once = hypridle 			                                        # IDLE


# Clipboard
exec-once = wl-paste --type text --watch cliphist store                 # Stores only text data
exec-once = wl-paste --type image --watch cliphist store                # Stores only image data

# Tray
exec-once = hyprctl dispatch exec "sleep 5s && nm-applet --indicator"   # Network Manager
exec-once = hyprctl dispatch exec "sleep 5s && blueman-applet"          # Bluetooth

# Personal      
exec-once = hyprctl dispatch exec "sleep 5s && 1password --silent"      # 1Password
exec-once = easyeffects --gapplication-service                          # EasyEffects

# XDG Fix
exec-once = systemctl --user restart xdg-desktop-portal-gtk.service     # XDG (GTK) restart
